[package]
name = "rsetup"
version = "0.1.0"
entrypoint = "rsetup.typ"
authors = ["James Bruce"]
license = "MIT"
description = "Knit R code into Typst documents — a Quarto alternative for statistics homework"
